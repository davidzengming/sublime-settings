{
    "shell_cmd": "g++ -std=gnu++17 -Wall \"${file}\" -o \"${file_path}/${file_base_name}\" && open -a Terminal.app ./${file_base_name}",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.c, source.c++",
    "shell": true,
}
